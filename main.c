#include<stdio.h>

int main ()
//Arthematic operators
{
    int a,b,multi,Div,Add,Sub;

printf("Enter the two value");
scanf("%d%d",&a,&b);
multi=a*b;
printf("Result of multi is %d\n",multi);

Div=a/b;
printf("result of Div is %d\n",Div);

Add=a+b;
printf("result of Add is %d\n",Add);

Sub=a-b;
printf("result of Sub id %d\n",Sub);
return 0;
}