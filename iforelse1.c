//check if number is postive else negative
#include<stdio.h>

int main(){
    int Num;
    printf("check the numbar is pos/neg");
    scanf("%d",&Num);

    if (Num >= 0, Num <= 0){
        printf("%d is negative.\n",Num);
    }
    else
    printf("%d is postive.\n",Num);

    return 0;
}